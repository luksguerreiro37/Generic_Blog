export function suggestion(){
    const suggestionContainer = document.querySelector('.suggestion');
    suggestionContainer.insertAdjacentHTML('afterbegin', '<h1 class="suggestion__title">Sugestões para você seguir');
}

